# See https://github.com/helm/chart-testing#configuration
remote: origin
target-branch: main
chart-dirs:
  - charts/
all: true
namespace: default
release-label: ct
# We need to disable this. Since app armor isn't available in GitHub Actions.
helm-extra-set-args: "--set containerSecurityContext.appArmorProfile=null"
